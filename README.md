# super-dev

A super project for the purpose of general software development.

## Local Virtual Machines

Available VMs can be found [here](./virtual-machines/README.md).

# Usage

First time cloning.

`git clone --recurse-submodules git@github.com:rokene/super-dev.git`

Update all submodules in project.

`git submodule update --init --recursive`
